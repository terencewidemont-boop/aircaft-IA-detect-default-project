
project:
  name: aircraft_surface_defect_detection
  seed: 42

data:
  image_size: 224
  batch_size: 16
  num_workers: 2
  train_split: 0.7
  val_split: 0.15
  test_split: 0.15

training:
  epochs: 30
  learning_rate: 0.0001
  weight_decay: 0.00001

model:
  num_classes: 2
